name: Build Tanks (MSYS2/MinGW)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-mingw:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup MSYS2 (MINGW64) and install packages
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          update: true
          install: |
            mingw-w64-x86_64-toolchain
            mingw-w64-x86_64-cmake
            mingw-w64-x86_64-SDL2

      - name: Configure CMake (MinGW Makefiles)
        shell: msys2 {0}
        run: |
          mkdir -p build
          cd build
          cmake -G "MinGW Makefiles" -DCMAKE_BUILD_TYPE=Release ..

      - name: Build with mingw32-make (capture logs)
        shell: msys2 {0}
        run: |
          cd build
          # capture build output to a log file so we can upload it even if build fails
          mingw32-make -j$(nproc) 2>&1 | tee build_log.txt || true

      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Tanks-mingw-logs
          path: build/build_log.txt

      - name: Upload Tanks.exe artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: Tanks-mingw
          path: build/**/Tanks.exe
